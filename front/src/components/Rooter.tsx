import React from 'react';

import {BrowserRouter , Router, Route} from "react-router-dom";
import { component } from "vue/types/umd";
import Home from "../components/Home"

const Rooter: React.FC = props => {
return(
    <BrowserRouter>
   <Router>
       
   </Router>
</BrowserRouter>
)
}

export default Rooter